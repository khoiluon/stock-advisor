# Scripts directory — Standalone entry points, không cần Django server.
#
# build_features.py  — raw parquet → features + labels parquet
# train_pipeline.py  — features parquet → 10 trained models (.joblib)
